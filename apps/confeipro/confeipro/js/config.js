// ═══════════════════════════════════════════
// CONFIG — marca do app (logo, nome, tagline, versão)
// ═══════════════════════════════════════════
// Esse é o arquivo pra trocar o "logo" (emoji + nome) e o texto
// que aparece no cabeçalho, sem precisar editar HTML nem caçar
// nada em outros arquivos. Troque os valores abaixo e recarregue.
//
// Pra trocar as CORES, é em css/tema.css.
const CONFIG = {
  emoji:       '🧁',                        // emoji do logo
  nomeBase:    'Confei',                    // primeira parte do nome
  nomeDestaque:'pro',                       // parte destacada (cor --accent)
  tagline:     'sua calculadora de preços', // subtítulo abaixo do nome
  versao:      'v1.3',                      // selo de versão no cabeçalho
};
